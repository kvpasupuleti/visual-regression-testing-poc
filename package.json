{
  "name": "visual-regression-testing-poc",
  "version": "1.0.0",
  "description": "Visual regression testing for web assessment platform",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html && cp _redirects dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "pixelmatch": "^5.3.0",
    "resemblejs": "^5.0.0"
  },
  "devDependencies": {
    "buffer": "6.0.3",
    "parcel": "^2.9.3"
  }
}
